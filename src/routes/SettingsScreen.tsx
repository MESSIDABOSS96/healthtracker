/**
 * Settings screen — Phase 1 minimal stub.
 * Plan 01-03 will extend with the Install card (D-12) and version line (D-10).
 * Phase 2 will add daily target inputs (calories, protein, carbs, fat, steps).
 */
export function SettingsScreen() {
  return (
    <div className="px-4 py-6 space-y-4">
      <h1 className="text-xl font-semibold">Settings</h1>
      {/* Plan 01-03 adds: Install card, Version line, Install handler wiring */}
    </div>
  );
}
