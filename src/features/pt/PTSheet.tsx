// src/features/pt/PTSheet.tsx
// Stub — Task 3 of Plan 02-04 overwrites this with the list ⇄ session composer.
// Kept minimal so PTSection can import it without a TS error during Task 1/2.

export function PTSheet(_props: { onClose: () => void }) {
  return null;
}
