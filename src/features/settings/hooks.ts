// src/features/settings/hooks.ts
// Populated by Plan 02-02 (goals + settings) — useLiveQuery wrappers around goals.svc.
// This placeholder file exists so downstream plans have a stable import path.
export {};
