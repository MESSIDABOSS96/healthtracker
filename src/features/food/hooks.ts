// src/features/food/hooks.ts
// Populated by Plan 02-03 (food slice) — useLiveQuery wrappers around meals.svc / food.svc.
// This placeholder file exists so downstream plans have a stable import path.
export {};
