// src/features/steps/hooks.ts
// Populated by Plan 02-05 (steps + lift + today) — useLiveQuery wrappers around steps.svc.
// This placeholder file exists so downstream plans have a stable import path.
export {};
