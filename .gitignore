# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Editor / OS
.DS_Store
*.local

# Env
.env
.env.*
!.env.example

# Dependencies
node_modules

# Build output
dist
dist-ssr

# Vite plugin PWA dev artifacts (Plan 01-03 will produce these)
dev-dist

# Test / coverage
coverage

# USDA source archive for scripts/build-food-table.mjs. The GENERATED table
# (src/data/foodTable.ts) is committed; this 6MB download is not, so a clean
# install never needs the network to build.
.cache

# Editor configs
.vscode/*
!.vscode/extensions.json
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.claude/worktrees/

# design-sync: staged converter scripts, generated bundle, and machine state.
# The durable inputs (.design-sync/config.json, NOTES.md, conventions.md,
# previews/, overrides/) ARE committed — everything below is regenerated.
.ds-sync/
ds-bundle/
.design-sync/.cache/
.design-sync/learnings/
.design-sync/node_modules

# Vercel CLI link state — machine-local, not shared
.vercel
